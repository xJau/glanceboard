[project]
name = "glanceboard-kindle"
version = "0.1.0"
description = "Daily calendar and weather board rendered for a jailbroken Kindle"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
filterwarnings = ["error::DeprecationWarning:glanceboard.*"]
